export enum Status {
  ACTIVE = "ACTIVE",
  INCOMPLETE = "INCOMPLETE",
  INACTIVE = "INACTIVE",
}

export enum ContactValidationType {
  SEND = "SEND",
  CONFIRM = "CONFIRM",
}

export enum ContactType {
  EMAIL = "EMAIL",
  PHONE = "PHONE",
}
